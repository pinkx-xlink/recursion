// fibonacci using iteration
let a = 0;
let b = 1;
// take in a number x
function fibs(x) {
    if(x < 2) {
        return 1
    } else {
        fibs(x) = fib(x-1) = fibs(x-2)
    }
    // run the fibonacci x times
    // return the result of the fib sequence after x iterations
}